name: mae
channels:
  - conda-forge
dependencies:
  # Compact default build for distribution. The CPU-only PyTorch variants
  # avoid bundling several gigabytes of CUDA runtime libraries.
  - python=3.12
  - timm
  - rasterio
  - fiona
  - "pytorch=*=cpu*"
  - "torchvision=*=cpu*"
  - pysimplegui
